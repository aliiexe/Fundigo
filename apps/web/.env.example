# =============================================================================
# SECURITY: Do NOT commit .env or .env.local to Git. Add .env*.local to .gitignore.
# Rotate Clerk and Supabase keys before production. Never commit secrets.
# =============================================================================

# --- Clerk (auth) ---
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_bGl2aW5nLXRvbWNhdC0yNy5jbGVyay5h2ZXYk
CLERK_SECRET_KEY=sk_test_qme9YWKFM3l2a4jLmastYCB10Q1DYQ81bYPKSdoDQV

# --- Supabase (server-side only; use for API routes and seeding) ---
# SECURITY: SUPABASE_SECRET_KEY must only be used server-side. Never expose to client.
SUPABASE_URL=<user_supabase_url>
SUPABASE_SECRET_KEY=<user_supabase_secret_key>

# --- Encryption (sensitive fields). Generate: openssl rand -base64 32 ---
# TODO: KMS (e.g. HashiCorp Vault) for production
MASTER_ENC_KEY=<user_master_key>

# --- Optional: OCR worker ---
OCR_WORKER_URL=http://localhost:3001
OCR_API_KEY=

# --- Optional: Sentry ---
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
